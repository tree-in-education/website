/*
const p1 = document.querySelector('.parallax-one');
const p2 = document.querySelector('.parallax-two');
const vw = Math.max(document.documentElement.clientWidth || 0, window.innerWidth || 0);
const vh = Math.max(document.documentElement.clientHeight || 0, window.innerHeight || 0);

console.log(vw, vh);


p1.style.backgroundSize = `${vh}px ${vh}px`;
p2.style.backgroundSize = `${vh}px ${vh}px`;
p2.style.backgroundPosition = `${vw / 2}px 55px`;
*/